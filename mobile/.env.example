# Required non-secret API origin. Production builds must use public HTTPS.
# Supply the real value through the build service environment; do not commit it.
EXPO_PUBLIC_API_BASE_URL=https://api.your-hugpong-domain.example
